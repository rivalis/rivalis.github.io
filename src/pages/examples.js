import React from 'react'
import Layout from '@theme/Layout'
import clsx from 'clsx'

export default () => (
    <Layout title="Examples" description="Open source NodeJS framework for building protocol agnostic and scalable multiplayer servers" >
        <header>
            <div className={clsx('container')}>
                <div className={clsx('row')}>
                    <div className={clsx('col col--12')}>
                        
                    </div>
                </div>
            </div>
        </header>
        <main>

        </main>
    </Layout>
)